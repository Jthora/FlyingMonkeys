import React from 'react';

import {ScreenShell} from '@/components/ScreenShell';

export const ReadyRoomScreen = (): React.JSX.Element => (
  <ScreenShell
    title="Ready Room"
    subtitle="Stage your deckpacks and calibrate the cadence."
  />
);
