import React from 'react';

import {ScreenShell} from '@/components/ScreenShell';

export const VaultScreen = (): React.JSX.Element => (
  <ScreenShell
    title="Vault"
    subtitle="Manage encrypted packs, backups, and duress protocols."
  />
);
