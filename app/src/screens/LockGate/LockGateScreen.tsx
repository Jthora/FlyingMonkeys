import React from 'react';

import {ScreenShell} from '@/components/ScreenShell';

export const LockGateScreen = (): React.JSX.Element => (
  <ScreenShell
    title="Lock Gate"
    subtitle="Authenticate and wake the vault shell."
  />
);
