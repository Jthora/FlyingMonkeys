import React from 'react';

import {ScreenShell} from '@/components/ScreenShell';

export const LiveStackScreen = (): React.JSX.Element => (
  <ScreenShell
    title="Live Stack"
    subtitle="Run the four-second loop and course-correct on the fly."
  />
);
